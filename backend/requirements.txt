# ============================================================
# Trident Agent MVP — Backend Dependencies
# ============================================================
# 安装: pip install -r requirements.txt
# ============================================================

# ── FastAPI + Server ────────────────────────────────────────
fastapi>=0.115.0
pydantic>=2.0
python-multipart>=0.0.9
uvicorn[standard]>=0.30.0

# ── Database ────────────────────────────────────────────────
aiosqlite>=0.20.0

# ── LLM Client (DeepSeek / OpenRouter / xAI / Doubao) ──────
openai>=1.50.0

# ── Excel Export ────────────────────────────────────────────
xlsxwriter>=3.1.0

# ── Environment Config ─────────────────────────────────────
python-dotenv>=1.0.0

# ── Crypto/market exchange adapter ────────────────────────
ccxt>=4.3.0
requests>=2.32.0

# ── Market structure indicators ───────────────────────────
# TA-Lib is the production decision engine. Native calculations remain
# observation/replay-only and fail closed when TA-Lib is unavailable.
TA-Lib>=0.7.1,<0.8

# ── TreeNews Bridge (treenews_bridge.py) ───────────────────
websocket-client>=1.7.0

# ⚠️ MetaTrader5 仅支持 Windows，Ubuntu 跳过
# MetaTrader5>=5.0
